# 志愿辅导课程素材目录

每门课程的图片、音频、PDF 等素材放在 `assets/<slug>/` 子目录下，对应 `data/tutoring.yml` 中的 `slug` 字段。

## 体积建议

- 图片：尽量压到 < 500KB
- 音频：MP3 64–96 kbps，建议单文件 < 20MB
- PDF：< 5MB

总仓库体积超过 100MB 后请考虑改用外部对象存储，只在 yml 里保留外链。
